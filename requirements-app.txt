# Dependências para BUILDAR o aplicativo (rtmlib-only).
# NÃO inclui torch/mmpose/mmcv/mediapipe (backends legados, não usados no app).
# O runtime CUDA (nvidia-*-cu12, ~1,7 GB) NÃO entra no pacote: é baixado do PyPI
# na 1ª execução pelo gpu_bootstrap, só se a máquina tiver GPU NVIDIA.
#
# Instalar num ambiente limpo:  python -m pip install -r requirements-app.txt

numpy==1.26.4
opencv-python==4.11.0.86
scipy==1.17.1
pandas==3.0.3
matplotlib==3.11.0
xlsxwriter==3.2.5
openpyxl==3.1.5

# Painel (app_marcha.py)
ttkbootstrap
pillow

rtmlib==0.0.15
onnxruntime-gpu==1.22.0

pyinstaller==6.21.0
